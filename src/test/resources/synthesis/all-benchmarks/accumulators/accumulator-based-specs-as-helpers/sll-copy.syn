should be able to synthesize list copy

#####

{ true; sll (x,s) ** ans :-> ori_loc ** sll(ori_loc, acc)}
void sll_copy_acc (loc x, loc ans)
{final_res == s ++ acc; ans :-> new_loc ** sll (new_loc, final_res) ** sll(x,s)}

{true ; r :-> x ** sll(x, s)<a>}
void sll_copy(loc r)
{true ; r :-> y ** sll(x, s)<a> ** sll(y, s)<b> }

#####


{r :-> x ** sll(x, s)<a>}
{r :-> y ** sll(x, s)<a> ** sll(y, s)<b>}
void sll_copy (loc r) {
  let x = *r;
  if (x == 0) {
  } else {
    let v = *x;
    let n = *(x + 1);
    *r = n;
    sll_copy(r);
    let y1 = *r;
    let y = malloc(2);
    *r = y;
    *(y + 1) = y1;
    *y = v;
  }
}


