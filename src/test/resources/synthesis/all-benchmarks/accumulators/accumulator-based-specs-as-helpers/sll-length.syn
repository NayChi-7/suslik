singly-linked list: length

#####

{ 0 <= n /\ 0<= acc; sll_bounded (x, n, s) ** ans :-> acc}
void sll_len_acc (loc x, loc ans)
{ final_res == acc + n; ans :-> final_res ** sll_bounded(x, n, s)}


{ 0 <= n ; ret :-> a ** sll_bounded(x, n, s) }
void sll_len (loc x, loc ret)
{ ret :-> n ** sll_bounded(x, n, s) }

#####

{0 <= n ; ret :-> a ** sll_bounded(x, n, s)<_alpha_14>}
{ret :-> n ** sll_bounded(x, n, s)<_alpha_15>}
void sll_len (loc x, loc ret) {
  *ret = 0;
  sll_len_acc(x, ret);
}

