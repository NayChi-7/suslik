
#####

{ 0 <= n1 && a1 == a + 1; ret :-> a1 ** sll_bounded(x, n1, s1) }
void sll_len (loc x, loc ret)
{ total == a1 + n1; ret :-> total ** sll_bounded(x, n1, s1) }

#####

void sll_len (loc x, loc ret) {
  let a = *ret;
  if (x == 0) {
  } else {
    let n = *(x + 1);
    *ret = a + 1;
    sll_len(n, ret);
  }
}